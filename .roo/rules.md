# Roo Code workspace rules for Lyra

Use the local Express API (`local-v1` on 127.0.0.1:4040). GHOST-HAND detailed mode is the default optimizer protocol. Cloudflare CI is dry-run only. Do not install GitHub Actions or GitHub MCP. Do not query NCIC/CJIS. Prefer FOLIO and the public-domain glossary instead of Black's Law Dictionary.

# Closest public Python deps for Lyra P1 inventory (research-note §3.2).
# Unpublished/wrong names from the research note are NOT listed as install targets.
# Map: crimemapping→folium, forensic-accounting→pyod, legal-ner→eyecite,
# db-forensics→sqlite-utils, steganography-tools→stegano, sec-edgar→edgartools,
# courtlistener→court-listener. pacer-client/openlaws stay REST stubs.
# apache-flink is intentionally omitted (TS anomaly classifier substitute).

folium>=0.15.0
pyod>=1.1.0
eyecite>=2.6.0
reporters-db>=3.0.0
sqlite-utils>=3.36
stegano>=0.11.0
pillow>=10.0.0
edgartools>=2.0.0
sec-edgar-downloader>=5.0.0
court-listener>=1.0.0
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
hachoir>=3.2.0
pefile>=2023.2.7
exifread>=3.0.0
sherlock-project>=0.15.0
